rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && (
        request.auth.token.email == 'nezasalton@gmail.com' ||
        request.auth.uid == 'S7QoMkUQNHaok4JjLB1fFd9OI0g1'
      );
    }

    function getUserData() {
      let path = /databases/$(database)/documents/users/$(request.auth.uid);
      return exists(path) ? get(path).data : {};
    }

    match /users/{userId} {
      allow read: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      allow create: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      allow update: if isAdmin() || (isSignedIn() && request.auth.uid == userId);
      allow delete: if isAdmin();
    }

    match /reports/{reportId} {
      // 1. ABSOLUTE ADMIN BYPASS
      // This ensures the Administrator can always query and manage all reports.
      allow read, write: if isAdmin();

      // 2. READ ACCESS for Leaders and Commanders
      allow read: if isSignedIn() && (
        let userData = getUserData();
        userData.role == 'COMMANDER' || 
        userData.role == 'LEADER'
      );

      // 3. UNIT-LOCKED ACCESS for Trainees
      // Trainees can only list/get reports within their own unit.
      allow read: if isSignedIn() && (
        resource.data.unit == getUserData().unit
      );
      
      // 4. CREATION
      allow create: if isSignedIn();
      
      // 5. MODIFICATION
      allow update: if isSignedIn() && (
        resource.data.unit == getUserData().unit ||
        resource.data.ownerId == request.auth.uid
      );
      
      // 6. DELETION (Leaders and above)
      allow delete: if isSignedIn() && (
        let userData = getUserData();
        userData.role == 'COMMANDER' || 
        userData.role == 'LEADER'
      );
    }
  }
}