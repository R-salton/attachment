
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // ABSOLUTE MASTER BYPASS
    function isMasterAdmin() {
      return isSignedIn() && (
        request.auth.uid == 'S7QoMkUQNHaok4JjLB1fFd9OI0g1' || 
        request.auth.uid == '7oiKVWSJ30Ucg0DxamaRhoxlI3G2' ||
        request.auth.token.email == 'nezasalton@gmail.com' ||
        request.auth.token.email == 'cboazi100@gmail.com'
      );
    }

    function isAdmin() {
      return isMasterAdmin() || (
        isSignedIn() && 
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'ADMIN'
      );
    }

    function isHighCommand() {
      return isAdmin() || (
        isSignedIn() && 
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
        (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'COMMANDER')
      );
    }

    match /users/{userId} {
      allow read, write: if isSignedIn() && (request.auth.uid == userId || isAdmin());
    }

    match /reports/{reportId} {
      // 1. GLOBAL COMMAND ACCESS
      // High Command can list and read everything
      allow read, list, delete: if isHighCommand();
      
      // 2. OWNER RESTRICTED ACCESS (Leaders and Trainees)
      // They can only list/read reports they created.
      allow get: if isSignedIn() && 
        (resource.data.ownerId == request.auth.uid || isHighCommand());
      
      allow list: if isSignedIn() && 
        (request.query.limit <= 100) &&
        (request.auth.uid == request.query.filters.ownerId || isHighCommand());
      
      // 3. FILING PROTOCOL
      allow create: if isSignedIn();
      allow update: if isSignedIn() && (resource.data.ownerId == request.auth.uid || isAdmin());
    }
  }
}
