/**
 * @fileoverview Firestore Security Rules for the Report Master application.
 *
 * Core Philosophy:
 * This ruleset prioritizes accessible data for reporting while restricting write access to authorized users.
 * It allows public read access to reports and master data, while restricting write operations to authenticated personnel.
 *
 * Data Structure:
 * - /companies/{companyId}: Master data for cadet companies.
 * - /operationalUnits/{unitId}: Master data for operational units (DPUs, SIF, etc.).
 * - /reports/{reportId}: Main daily aggregate reports.
 * - /reports/{reportId}/... (subcollections): Detailed operational data, challenges, and recommendations.
 *
 * Key Security Decisions:
 * - Public read access is allowed for reports and master data to facilitate transparency and review.
 * - Write operations (create, update, delete) are restricted to authenticated users.
 * - No user listing is allowed at the root level.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Master data for companies. Publicly readable, authenticated writes.
     */
    match /companies/{companyId} {
      allow get, list: if true;
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Master data for operational units. Publicly readable, authenticated writes.
     */
    match /operationalUnits/{unitId} {
      allow get, list: if true;
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Access control roles. Restricted to the specific user.
     */
    match /roles_cadet_commanders/{commanderId} {
      allow get: if isSignedIn() && request.auth.uid == commanderId;
      allow list, write: if false;
    }

    /**
     * @description Main reports collection. Publicly readable, authenticated writes.
     */
    match /reports/{reportId} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && resource != null;

      // Recursive match for all subcollections under a report
      match /{allSubcollections=**} {
        allow get, list: if true;
        allow create: if isSignedIn();
        allow update, delete: if isSignedIn() && resource != null;
      }
    }

    //----------------------------------------------------------------------------------------------
    //  Helper Functions
    //----------------------------------------------------------------------------------------------

    /**
     * @description Checks if the user is signed in.
     * @return {bool} True if the user is signed in, false otherwise.
     */
    function isSignedIn() {
      return request.auth != null;
    }
  }
}